#include <stdio.h>
#include <stdlib.h>

typedef struct node {
    int number;
    struct node *next;
} node;

int main(void) {
    // Create an empty list
    node *list = NULL; // The list is the address of the first node in the list
}
